{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "pelham/taxes.schema.json",
  "title": "Your Taxes panel",
  "description": "The /taxes page: a bar visual of how a Pelham property tax bill divides between governing bodies, derived from Westchester County's published 2025/2026 rates; explainer blocks describing what each body's share funds; a like-for-like Village of Pelham vs Pelham Manor comparison; and outbound Learn more links. Prose fields accept the safe HTML subset and {{fact:<id>}} tokens.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "bars",
    "explainers"
  ],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Relative path to this schema file."
    },
    "section_intro": {
      "type": "string",
      "description": "Lead paragraph above the visual."
    },
    "note": {
      "type": "string",
      "description": "The sourcing note under the bars: where the percentages come from and that they are approximate."
    },
    "comparison": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "title",
        "entries"
      ],
      "description": "Side-by-side village comparison below the breakdown. Each amount is rendered from its fact, and the bar lengths are computed from the facts' numeric values.",
      "properties": {
        "title": {
          "type": "string",
          "description": "Heading, e.g. 'Village of Pelham vs Village of Pelham Manor'."
        },
        "subtitle": {
          "type": "string",
          "description": "The basis of the comparison — the shared assessed value. A comparison without it is not like-for-like."
        },
        "note": {
          "type": "string",
          "description": "Caveat and source under the comparison."
        },
        "entries": {
          "type": "array",
          "minItems": 2,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "id",
              "label",
              "fact_ref",
              "fill_style"
            ],
            "properties": {
              "id": {
                "type": "string",
                "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
              },
              "label": {
                "type": "string"
              },
              "fact_ref": {
                "type": "string",
                "description": "facts.json id. Its numeric `value` sizes the bar and its `display` is printed. Build fails on an unknown id or a non-numeric value."
              },
              "fill_style": {
                "enum": [
                  "school",
                  "village",
                  "manor",
                  "county",
                  "town"
                ]
              },
              "governing_body": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "Foreign key to bodies.json id."
              }
            }
          }
        }
      }
    },
    "bars": {
      "type": "array",
      "minItems": 1,
      "description": "DISPLAY ORDER IS ARRAY ORDER, largest share first.",
      "items": {
        "$ref": "#/$defs/bar"
      }
    },
    "explainers": {
      "type": "array",
      "minItems": 1,
      "description": "DISPLAY ORDER IS ARRAY ORDER. Deliberately a different order from `bars` — the pre-refactor page listed the bars as school/village/county/town but the explainers as school/county/village/town. Kept as two independent arrays rather than forced into one order.",
      "items": {
        "$ref": "#/$defs/explainer"
      }
    },
    "learn_more": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "links"
      ],
      "description": "Outbound reading at the foot of the page, for readers who want the primary source rather than this site's summary of it. Rendered last.",
      "properties": {
        "title": {
          "type": "string",
          "description": "Section heading, e.g. 'Learn more'."
        },
        "links": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "id",
              "label",
              "url"
            ],
            "properties": {
              "id": {
                "type": "string",
                "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
              },
              "label": {
                "type": "string",
                "description": "Link text. Say what the reader gets, not just the publisher."
              },
              "url": {
                "type": "string",
                "description": "Destination. External, so it opens in a new tab."
              },
              "note": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "Short line under the link naming the source."
              }
            }
          }
        }
      }
    }
  },
  "$defs": {
    "bar": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "label",
        "percent_text",
        "bar_width",
        "fill_style"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
          "description": "Stable slug for the row."
        },
        "label": {
          "type": "string",
          "description": "Row name as printed, without the icon."
        },
        "icon": {
          "type": [
            "string",
            "null"
          ],
          "description": "Emoji rendered before the label."
        },
        "percent_text": {
          "type": "string",
          "description": "The text printed at the right of the row. A string, not a number: shares are derived from published rates, so they are approximate ('~68% ✓'), and the town share is an estimate ('~1-2% est.') because its rate is not separately published. Mark derived-from-published shares with ✓ and estimates with 'est.'."
        },
        "detail": {
          "type": "string",
          "description": "Optional second line under the row name — the body's published rate, via a {{fact:*-tax-rate-*}} token. Safe HTML subset plus tokens."
        },
        "bar_width": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 0,
          "maximum": 100,
          "description": "CSS width percentage of the filled bar — the share rounded to a whole number (county ~3-4% draws as 4). Keep it in step with percent_text and the matching *-share-of-tax-bill fact. Null draws an empty, hatched track, for a body whose share has no figure at all."
        },
        "fill_style": {
          "enum": [
            "school",
            "village",
            "manor",
            "county",
            "town"
          ],
          "description": "Bar colour; renders as CSS class .bar-<value>. Must match an existing rule in the stylesheet."
        },
        "governing_body": {
          "type": [
            "string",
            "null"
          ],
          "description": "Foreign key to bodies.json id. Null for a row covering more than one body."
        }
      }
    },
    "explainer": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "title",
        "description",
        "accent"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
          "description": "Stable slug for the block."
        },
        "title": {
          "type": "string",
          "description": "Block heading, without the icon."
        },
        "icon": {
          "type": [
            "string",
            "null"
          ],
          "description": "Emoji rendered before the title."
        },
        "description": {
          "type": "string",
          "description": "What this body's share of the bill actually funds. Safe HTML subset plus {{fact:<id>}} tokens — write {{fact:village-budget-fy2627}}, never a literal dollar figure."
        },
        "accent": {
          "enum": [
            "none",
            "gold",
            "red",
            "green"
          ],
          "description": "Block accent colour; renders as CSS class .<value> on .tax-explainer-block ('none' emits no extra class). Presentation only, and deliberately not aligned to the bar colours — the pre-refactor page used a different colour per block than per bar."
        },
        "link": {
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": false,
          "required": [
            "label",
            "url"
          ],
          "description": "The source link at the foot of the block.",
          "properties": {
            "label": {
              "type": "string",
              "description": "Link text, e.g. 'School budget documents'."
            },
            "url": {
              "type": "string",
              "description": "Destination."
            }
          }
        },
        "governing_body": {
          "type": [
            "string",
            "null"
          ],
          "description": "Foreign key to bodies.json id."
        }
      }
    }
  }
}
